// PreProcessor Command [Tells compiler to include file named stdio.h]
# include <stdio.h> 
//Main function [Starting of a Program]; int returns return 0, here 0 being the integer
int main() 
{
    int a;
    // printf is from stdio.h and prints the content inside () AND ";" being the terminator of the statement
    // printf("Lorem Ipsum\n");
    printf("Lorem Ipsum");

    /* scanf("%d",a);
    printf("You had Entered %d",a,"above\n"); */ 
    /*If all of the above statements are compiled correctly, then it will return 0 as off command to the OS */
    return 0; 
}